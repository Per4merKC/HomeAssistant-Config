---
recorder:
  db_url: !secret mariadb_url
  purge_keep_days: 10
  include:
    entities:
      - sensor.duolingo_streak
  exclude:
    domains:
      - sun
    entities:
      - sensor.chiefs_tracker
      - sensor.local_time
      - sensor.mun_tracker
      - sensor.royals_tracker
      - sensor.sporting_tracker
